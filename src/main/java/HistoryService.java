import javax.xml.soap.Detail;
import java.util.ArrayList;
import java.util.stream.Stream;

public class HistoryService {
    public Stream<Integer> getDetailts(String productId) {
        return new ArrayList<Integer>().stream();
    }
}
